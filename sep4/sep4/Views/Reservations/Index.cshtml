@model IEnumerable<sep4.Reservation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FromDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ToDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sauna.TemperatureThreshold)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FromDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ToDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sauna.TemperatureThreshold)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { SaunaID=item.SaunaID, UserID=item.UserID, FromDateTime=item.FromDateTime, ToDateTime=item.ToDateTime }) |
            @Html.ActionLink("Details", "Details", new { SaunaID = item.SaunaID, UserID = item.UserID, FromDateTime = item.FromDateTime, ToDateTime = item.ToDateTime }) |
            @Html.ActionLink("Delete", "Delete", new { SaunaID = item.SaunaID, UserID = item.UserID, FromDateTime = item.FromDateTime, ToDateTime = item.ToDateTime })
        </td>
    </tr>
}

</table>
